{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "uv run python -m scheduler",
    "cronSchedule": "0 12 * * 1-7",
    "restartPolicyType": "NEVER"
  }
}
