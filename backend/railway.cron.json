{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "uv run python -m scheduler",
    "cronSchedule": "0 23 * * 1-5",
    "restartPolicyType": "NEVER"
  }
}
