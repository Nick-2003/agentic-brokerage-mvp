# Copy this file to .env and fill in the values.
# Get keys via docs/ACCOUNTS_SETUP_GUIDE.md
# NEVER commit .env (.gitignore already excludes it).

# ── Claude API ──
ANTHROPIC_API_KEY=sk-ant-api03-REPLACE
# Default model. Change to a specific dated ID if needed.
ANTHROPIC_MODEL=claude-opus-4-5

# ── Alpaca paper trading (real broker, paper money) ──
ALPACA_API_KEY=PK_REPLACE
ALPACA_API_SECRET=REPLACE
ALPACA_BASE_URL=https://paper-api.alpaca.markets

# ── Holdings (Interactive Brokers Flex Web Service — W1, read-only) ──────────
# Powers the daily WhatsApp briefing's "what moved" layer (the pivot product).
# Read-only XML reports via a 2-step flow: SendRequest(token, queryId) -> a
# ReferenceCode, then GetStatement(ReferenceCode) -> the statement XML.
# Set up an *Activity* Flex Query (Account Mgmt → Performance & Reports → Flex
# Queries) with these sections: Open Positions + Net Asset Value (NAV) in Base +
# Mark-to-Market Performance Summary in Base (+ Change in NAV, Month & Year to
# Date Performance, Realized & Unrealized Performance, Financial Instrument
# Information). Delivery: Format=XML, Period=Last Business Day. General config:
# Enable Base Currency P&L=ON, Include Currency Rates=Yes, Include Audit Trail
# Fields=No, date format yyyy-MM-dd, Breakout by Day=No.
# Then create a Flex Web Service token (Reporting → Settings → Flex Web Service).
# USE_MOCK_IBKR=1 parses the bundled backend/data/mock_flex_statement.xml (offline
# demo). Unset it AND set a real token + query id to pull a live statement.
USE_MOCK_IBKR=1
IBKR_FLEX_TOKEN=REPLACE
IBKR_FLEX_QUERY_ID=REPLACE
IBKR_FLEX_TIMEOUT_S=30

# ── Main-page portfolio source + trading (039 + 040 per-user) ────────────────
# Which broker feeds the main page (the Hero "Portfolio value" + the agent's
# `morning_brief`). `ibkr` (default) = read-only IBKR Flex holdings in the account's
# BASE currency. `alpaca` = the legacy Alpaca paper path.
# 040: the IBKR path is PER-USER — it reads the signed-in user's OWN /connect IBKR
# connection (encrypted token in `ibkr_connections`, via the service key). A user who
# hasn't connected sees a NIL portfolio (no fixture). So the IBKR_FLEX_* env creds
# above are NOT used for the main page anymore (they remain a dev/probe default for
# `ibkr_flex` directly). USE_MOCK_IBKR=1 still makes a *connected* user's fetch return
# the bundled fixture (for offline dev); not-connected stays nil regardless.
# Requires SUPABASE_SERVICE_KEY (the per-user connection read).
PORTFOLIO_SOURCE=ibkr
# IBKR Flex is a slow, end-of-day statement service → cache the snapshot per-user (seconds).
IBKR_PORTFOLIO_CACHE_TTL_S=600
# Order placement. OFF by default with PORTFOLIO_SOURCE=ibkr (Flex can't trade, so
# `place_paper_order` returns `trading_unavailable` and the agent says so). Set to 1
# (with PORTFOLIO_SOURCE=alpaca) to re-enable the Alpaca paper-order path.
TRADING_ENABLED=0

# Sample portfolio (053) — GUEST/DEMO only. When enabled, an unauthenticated/demo
# visitor sees a read-only DEDICATED Alpaca PAPER book (clearly badged "Sample")
# instead of an empty hero. Signed-in users are never shown this (they get their
# own IBKR account or nil). Default OFF → no behaviour change until set.
# Use a DEDICATED Alpaca paper account (NOT your operator ALPACA_* keys); seed it
# with a few holdings (scripts/seed_sample_alpaca.py). If enabled but creds are
# absent, the static MOCK_PORTFOLIO is shown (still badged "Sample").
SAMPLE_PORTFOLIO_ENABLED=0
SAMPLE_ALPACA_API_KEY=PK_REPLACE
SAMPLE_ALPACA_API_SECRET=REPLACE

# ── Briefing generator (W2 — the daily WhatsApp narrative) ───────────────────
# System-side generator (NOT an agent tool): Flex holdings (W1) + market context
# → a single tool-less Claude call → WhatsApp-ready prose. Reuses ANTHROPIC_API_KEY
# + ANTHROPIC_MODEL above.
# USE_MOCK_BRIEFING=1 (or no ANTHROPIC_API_KEY) → deterministic template prose,
# no API spend (offline demo + the W2 offline test). Unset it (with a key set) to
# have Claude write the narrative. Independent of USE_MOCK_IBKR — you can write a
# real Claude brief over the mock Flex fixture.
USE_MOCK_BRIEFING=1
# Optional: route the narrative to a cheaper model than the chat ANTHROPIC_MODEL.
# BRIEFING_MODEL=claude-haiku-4-5-20251001
BRIEFING_MAX_CHARS=1500
BRIEFING_TOP_MOVERS=4
# Headline freshness window (days), anchored to the statement's as_of date: a
# mover's news must be at/after (as_of − this) to be offered as a cause, so a
# stale headline can't explain today's move. Raise it if quiet-day briefs lose
# too much "why"; lower it for stricter freshness.
BRIEFING_NEWS_MAX_AGE_DAYS=3
# Display timezone for the brief's freshness note (052). The note shows the
# generation/send time in BOTH this zone AND GMT so a reader (e.g. in GMT+8) can
# judge recency at a glance. Any IANA zone name; falls back to UTC if unavailable.
BRIEFING_TZ="Asia/Hong_Kong"

# ── WhatsApp delivery (W3 — Twilio, system-side; NEVER an agent tool) ────────
# Sends the daily briefing over WhatsApp. The scheduler (W5) imports
# backend/whatsapp.py and calls it; the agent has no send tool (SECURITY threat 1).
# USE_MOCK_WHATSAPP=1 (or missing creds) → LOG the message instead of sending
# (offline dev; lets the W5 cron be tested without spending/spamming). `twilio` is
# only needed for real sends: `uv sync --group whatsapp`.
# Sandbox (validation cohort): Twilio Console → Messaging → Try it out → Send a
# WhatsApp message; each tester sends the "join <phrase>" code once (opt-in), then
# TWILIO_WHATSAPP_FROM is the sandbox number. Production (W6) needs a registered
# WhatsApp Business sender + approved templates. Full guide: .self_management/TWILIO_SETUP.md.
USE_MOCK_WHATSAPP=1
TWILIO_ACCOUNT_SID=AC_REPLACE
TWILIO_AUTH_TOKEN=REPLACE
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
# W6 — inbound STOP/START webhook (POST /api/twilio/inbound). Twilio signs each
# request with X-Twilio-Signature; we validate it with TWILIO_AUTH_TOKEN (anti-
# spoof). Behind a proxy set TWILIO_WEBHOOK_URL to the EXACT public URL Twilio
# calls — else validation fails on a scheme/host mismatch. TWILIO_WEBHOOK_VALIDATE=0
# skips validation in local dev (no token).
TWILIO_WEBHOOK_VALIDATE=1
# TWILIO_WEBHOOK_URL=https://your-backend.example.com/api/twilio/inbound
# W6.1b — Twilio error codes meaning "recipient opted out (texted STOP) / blocked".
# Twilio intercepts the STOP keyword (the inbound webhook never sees it) but blocks
# the number; the block surfaces ASYNCHRONOUSLY as a delivery status (failed/
# undelivered + one of these codes) → POST /api/twilio/status flips opt_in off.
# 63015 = sandbox recipient-not-joined (what a STOP gives in the sandbox, confirmed
# live); 63024 = WhatsApp not opted in; 21610 = opted out. Drop 63015 in production.
TWILIO_OPTED_OUT_CODES=21610,63024,63015
# Delivery-status callback URL — set per-message on sends so async opt-out/block
# results reach the status endpoint. Must be the EXACT public URL (signature match),
# e.g. your ngrok/Railway base + /api/twilio/status. Unset → no callback (no async mirror).
# TWILIO_STATUS_CALLBACK_URL=https://your-backend.example.com/api/twilio/status
# TWILIO_BRIEF_TEMPLATE_SID=HX…………        # the approved Content template SID from W6.4a Step C
# TWILIO_WHATSAPP_FROM=whatsapp:+<biz>     # the registered Business sender (replaces the sandbox number)

# ── Daily briefing scheduler (W5 — the cron job) ─────────────────────────────
# scripts/run_briefings.py iterates every opted-in connection → build (W2) →
# send (W3) → log (W4). Per-user failures are isolated + logged; transient
# build/send errors retry on this backoff (comma-separated seconds; empty = none).
BRIEFING_RETRY_DELAYS=3,8
# Cost ceiling: max users (≈ max Claude calls) per run. Each user = one brief.
BRIEFING_MAX_USERS_PER_RUN=100

# ── Brief web permalink (W6.3) ───────────────────────────────────────────────
# Each brief is published under a capability token + served at PUBLIC_BASE_URL/b/<token>
# (GET /api/brief/{token}); the scheduler appends the link to the message (W6.4's
# template will use it too). Body is token-gated + expiring (service-role-only RLS).
# PUBLIC_BASE_URL = the FRONTEND origin (where /b/<token> lives): localhost:3000 in
# dev, your Vercel URL in prod. PUBLISH_BRIEFS=0 disables publishing.
PUBLIC_BASE_URL=http://localhost:3000
BRIEF_PERMALINK_TTL_DAYS=7
PUBLISH_BRIEFS=1

# ── Email briefing channel (038 — Resend; P7-NOTIFY email-first) ──────────────
# A SECOND delivery channel for the daily brief, alongside WhatsApp. System-side
# (scheduler only; NEVER an agent tool). Per-user opt-in (`email_opt_in`, separate
# from the WhatsApp opt-in). Mock-first: USE_MOCK_EMAIL=1 (or missing creds) LOGS
# the email instead of sending. A REAL send needs BOTH the Resend creds below
# AND an unsubscribe secret (no unsubscribe link → no send). EMAIL_BRIEFINGS=0
# disables the whole leg.
USE_MOCK_EMAIL=1
# Resend (permanent free tier: 3,000 emails/mo, 100/day — Twilio funding does NOT
# subsidise SendGrid, so Resend's standalone free tier wins). Create an API key at
# resend.com → API Keys (Sending access only). EMAIL_FROM must be on a domain you've
# verified in Resend (DNS: SPF + DKIM TXT records), else Resend 403s. For a quick
# test only, Resend's onboarding@resend.dev sender delivers to your own signup email.
RESEND_API_KEY=re_REPLACE
EMAIL_FROM=briefings@yourdomain.example
EMAIL_FROM_NAME="Daily Portfolio Briefing"
# EMAIL_HTTP_TIMEOUT=15
# One-click unsubscribe (RFC 8058). Signs the per-user unsubscribe token; with it
# unset, real email won't send. Mint any high-entropy string, e.g.
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
EMAIL_UNSUBSCRIBE_SECRET=REPLACE
# Where the unsubscribe link points. Prefer PUBLIC_BACKEND_URL (the Railway backend,
# where /api/email/unsubscribe lives — no proxy hop). If unset, it falls back to
# PUBLIC_BASE_URL (frontend), which works via the 038 /api/email/* rewrite.
# PUBLIC_BACKEND_URL=https://your-backend.example.com
# EMAIL_BRIEFINGS=1

# ── IBKR connect storage (W4 — encrypted Flex token at rest) ─────────────────
# The connect flow stores each user's Flex token ENCRYPTED (app-side Fernet,
# backend/token_crypto.py — uses the existing `cryptography` dep, no new package).
# Mint a key: `cd backend && .venv/bin/python -m token_crypto` → paste below.
# Rotating the key invalidates stored ciphertext. NEVER commit a real key.
FLEX_TOKEN_ENC_KEY=REPLACE
# NOTE: the W5 cron reads ALL opted-in connections via the Supabase SERVICE KEY
# (service_role bypasses RLS — the one legitimate admin path, a backend job, never
# a user request). Set SUPABASE_SERVICE_KEY below for that system read. The
# connect ENDPOINT itself uses the user's JWT + RLS, not the service key.

# ── Supabase (auth + Postgres) ──
SUPABASE_URL=https://REPLACE.supabase.co
SUPABASE_ANON_KEY=eyJREPLACE
SUPABASE_SERVICE_KEY=eyJREPLACE
# JWT verification (P4.1 / proposal 015). The backend verifies the access token
# by its header `alg`:
#   • Asymmetric "JWT Signing Keys" (ES256/RS256 — Supabase's current default):
#     verified against the project's published public keys (JWKS) at
#     <SUPABASE_URL>/auth/v1/.well-known/jwks.json. Needs SUPABASE_URL above
#     (+ SUPABASE_ANON_KEY, sent as the `apikey` header). No secret required.
#   • Legacy symmetric secret (HS256): set SUPABASE_JWT_SECRET below
#     (Supabase → Settings → API → "JWT Secret"). Only needed for HS256 projects.
SUPABASE_JWT_SECRET=REPLACE

# ── Auth enforcement (P4.1) ──
# REQUIRE_AUTH=1 → reject unauthenticated POST /api/chat (401) and derive
# user_id from the JWT, killing the spoofable "demo" default (SECURITY_AUDIT
# HIGH-2). SET THIS TO 1 IN PRODUCTION (Railway).
# Unset/0 (local) → if a Bearer token is sent it's still verified, but a request
# WITHOUT a token falls back to the "demo" user — so the deterministic mock demo,
# scripts/smoke_test.sh and curl checks keep working.
REQUIRE_AUTH=0

# ── Langfuse observability (P4.4 / proposal 017) ──
# Per-turn traces for the agent loop (one root span "chat", a `generation`
# child per Anthropic call, a `tool:<name>` child per tool call). Tagged with
# the authenticated user_id and the conversation_id (as Langfuse session_id).
# Unset / placeholder → tracing is a silent no-op; nothing fails.
LANGFUSE_PUBLIC_KEY=pk-lf-REPLACE
LANGFUSE_SECRET_KEY=sk-lf-REPLACE
# ^ Region (default = US cloud). Common values:
#   https://cloud.langfuse.com   (US)
#   https://eu.cloud.langfuse.com (EU)
#   https://jp.cloud.langfuse.com (JP / APAC)
#   http://localhost:3000        (self-hosted; conflicts with the frontend port if both run locally)

# ── Mem0 memory (P4.3 / proposal 025) ──
# Per-user fact recall across conversations. Before each turn the backend
# searches THIS user's memories (scoped by the authenticated user_id) and
# injects them into the system prompt; after the turn it stores new salient
# facts. Scope is ALWAYS the verified Supabase UUID — never a client value.
# Requires the optional `memory` dependency group: `uv sync --group memory`.
# Unset / placeholder → memory is a silent no-op; nothing fails.
MEM0_API_KEY=m0-REPLACE
# Max memories injected per turn (bounds system-prompt token growth).
MEM0_SEARCH_LIMIT=5
# Hard cap on total characters of the injected memory block.
MEM0_MAX_CHARS=1500

# ── PostHog (analytics) ──
POSTHOG_API_KEY=phc_REPLACE
POSTHOG_HOST=https://us.i.posthog.com

# ── TradingView MCP (P1.2 — local-dev / demo only) ─────────────────────────
# Real chart manipulation requires TradingView Desktop running on the SAME
# machine with --remote-debugging-port=9222 enabled. In Railway production
# there is no TV Desktop, so leave USE_MOCK_TA=1 there.
# See README.md "Talk-to-your-charts" for one-time setup.
#
# LIMITATION: USE_MOCK_TA=0 alone is NOT enough — TradingView Desktop must
# ALSO be open with the debug port. Launch it with:
#   open -a "TradingView" --args --remote-debugging-port=9222
# Verify: `curl -s localhost:9222/json/version` (look for "TVDesktop" in the UA).
# If TV Desktop is closed while USE_MOCK_TA=0, the chart degrades to mock data
# (now labelled `TradingView (mocked — live data unavailable)`, proposal 029).
USE_MOCK_TA=1
TRADINGVIEW_MCP_COMMAND=node
# Absolute path to the sibling-cloned tradesdontlie/tradingview-mcp repo's server.
# Example: /Users/you/Code/tradingview-mcp/src/server.js
TRADINGVIEW_MCP_ARGS=REPLACE_WITH_ABSOLUTE_PATH/tradingview-mcp/src/server.js
TRADINGVIEW_MCP_CDP_PORT=9222

# ── Research data (P2b — Financial Modeling Prep) ───────────────────────────
# FMP is the research/fundamentals provider behind get_full_research /
# get_consensus_targets / get_company_fundamentals / get_peer_set.
# USE_MOCK_RESEARCH=1 forces the hand-tuned 7-ticker mocks (deterministic demo).
# Unset it AND set a real FMP_API_KEY to cover any US ticker.
USE_MOCK_RESEARCH=1
FMP_API_KEY=REPLACE

# ── Options chain (077 — get_option_chain, data-only) ───────────────────────
# yfinance options: strikes, bid/ask/last, implied vol, open interest (~15-min
# delayed). NO Greeks (data source has none — the tool says so). Mock-first:
# USE_MOCK_OPTIONS=1 (or USE_MOCK_MARKET=1, or no yfinance) → a deterministic
# sample chain (NVDA/AAPL). Unset it to hit real yfinance for any US ticker.
# No API key — yfinance is keyless.
USE_MOCK_OPTIONS=1
FMP_TIMEOUT_S=10

# ── News supplement (060 — Alpha Vantage NEWS_SENTIMENT) ────────────────────
# BRIEFING-ONLY supplement to yfinance headlines (the chat news tool stays
# yfinance). Set on the Railway CRON/briefing service only. Enabled iff
# USE_MOCK_NEWS != 1 AND a real ALPHAVANTAGE_API_KEY is set. Free tier = 25
# req/day → the TTL cache + soft daily cap keep it under that; over cap or any
# error silently falls back to yfinance-only. Leave as REPLACE to stay off.
ALPHAVANTAGE_API_KEY=REPLACE
ALPHAVANTAGE_TIMEOUT_S=10
ALPHAVANTAGE_DAILY_CAP=22
ALPHAVANTAGE_CACHE_TTL_S=600

# ── Runtime config ──
CORS_ALLOW_ORIGINS=http://localhost:3000
MAX_TOOL_ITERATIONS=10
PER_USER_RATE_LIMIT_PER_MIN=30
MAX_PAPER_TRADE_NOTIONAL=50000

CHAT_DAILY_TOKEN_BUDGET=200000     # per-user tokens/UTC-day; 0 or unset = disabled

# 065 — chat error verbosity. Default (1/unset) shows the specific provider error
# + reason in the chat bubble (e.g. "Anthropic API — billing/credits: your credit
# balance is too low …") so you can diagnose during pre-launch. Set to 0 to show
# only generic, non-leaky messages once real/untrusted users are on it.
CHAT_VERBOSE_ERRORS=1

# 067 — widget numeric-provenance validator (trust #1/#3). Every hard market/
# account number in a widget (prices, fills, P&L, analyst target) must trace back
# to a tool result from that turn.
#   off     = no checking
#   warn    = DEFAULT. Check + log/trace violations, still render the widget.
#             Run here first to measure the false-positive rate on real traffic.
#   enforce = fail closed — suppress the widget and return `widget_unverified`.
# Flip to `enforce` once `warn` shows a clean baseline. Proposal 066's DeepSeek
# fallback must NOT be enabled until this is `enforce`.
WIDGET_VALIDATOR_MODE=enforce

# ── 083 — LLM transport policy (applies to EVERY OpenAI-format rail) ────────
# One shared implementation in `backend/llm_transport.py`, used by kimi_client,
# deepseek_client and openai_client. Three vars per rail, same meaning each time:
#
#   <RAIL>_TIMEOUT_S          TOTAL wall-clock budget for ONE model call, retries
#                             INCLUDED. It is a deadline, not a per-attempt read
#                             timeout, so raising MAX_ATTEMPTS can never multiply
#                             how long a user waits. Default 120.
#   <RAIL>_CONNECT_TIMEOUT_S  Separate, short connect timeout (default 10) so an
#                             unreachable host fails fast instead of eating the
#                             whole budget. Only GENERATION should be slow.
#   <RAIL>_MAX_ATTEMPTS       Total attempts, not extra retries (default 2; 1
#                             disables). Retries only `network`/`overloaded`, and
#                             only with >=15s of budget left. A read timeout is
#                             never retried — it has already spent the budget.
#
# ⚠️ WHY THIS CHANGED: 60s was measured to be right at the edge of a healthy Kimi
# turn (live iterations ~55s), and a read timeout surfaced to the user as
# `KimiError: kimi request failed:` with an EMPTY message — httpx maps timeouts
# from a message-less httpcore exception, so `{e}` interpolated to nothing.
# ⚠️ AN ENV VALUE BEATS THE NEW CODE DEFAULT. If a deployment already sets
# <RAIL>_TIMEOUT_S=60, it stays 60. Update it on BOTH Railway services (web AND
# cron — they never share variables); the daily brief calls the same clients.

# 069 — DeepSeek fallback rail. Used only when Anthropic fails on a usage-limit
# error AND LLM_FALLBACK_ENABLED=1. Mock-first: real path needs a key + mock off.
# ⚠️ Keep LLM_FALLBACK_ENABLED=0 until WIDGET_VALIDATOR_MODE=enforce — a weaker
# model with no trust validator would render fabricated numbers. And the loop
# wiring itself (069 phase 2) ships only after scripts/deepseek_probe.py confirms
# deepseek-chat tool-calling. Turns with image attachments never fall back (no vision).
DEEPSEEK_API_KEY=REPLACE
DEEPSEEK_MODEL=deepseek-chat        # NOT deepseek-reasoner (no function calling)
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_TIMEOUT_S=120             # 083 — TOTAL budget per call (was a 60s per-attempt read timeout)
DEEPSEEK_CONNECT_TIMEOUT_S=10
DEEPSEEK_MAX_ATTEMPTS=2
USE_MOCK_DEEPSEEK=1
LLM_FALLBACK_ENABLED=0
# 083 — `timeout,network` added: a transport failure matched NONE of the reasons
# below, so a Kimi timeout ended the turn while a funded DeepSeek sat unused.
# Drop them here to restore the pre-083 fail-fast behaviour.
LLM_FAILOVER_ON=billing,rate_limit,overloaded,timeout,network

# 070 — DeepSeek fallback for the DAILY BRIEF (cron service). Defaults to whatever
# LLM_FALLBACK_ENABLED is, so one flag normally arms both chat + brief. Set to 0 to
# keep the chat fallback while refusing to SEND a DeepSeek-written brief to users
# (external comms = higher bar). The delivered brief says which model wrote it.
# NOTE: set DEEPSEEK_API_KEY on the CRON service too — it's a separate Railway
# service from the web backend and does not inherit its variables.
# BRIEFING_FALLBACK_ENABLED=1
# ─────────────────────────────────────────────────────────────────────────────
# 071 — OpenAI rail (selectable PRIMARY). Mock-first: the real path needs a key
# AND mock off. Added by 073 — 071 shipped the code but never documented these.
#
# Why this rail exists: 068 confirmed Claude-via-Vertex is geo-ineligible for
# this HK-billed account and Anthropic-direct credits are exhausted, so DeepSeek
# was about to become the permanent primary. OpenAI is vision-capable (059 keeps
# working) and US/EU-jurisdiction, which DeepSeek is not.
#
# ⚠️ OPENAI_MODEL below is a PLACEHOLDER, not a verified id — model ids move.
#    Run `scripts/openai_probe.py --list` to see what your key reaches, then
#    `--model <id> --vision`. Do NOT set LLM_RAIL=openai until tool-calling is
#    green: the agent loop is useless without it.
OPENAI_API_KEY=REPLACE
OPENAI_MODEL=gpt-5                 # ⚠️ CONFIRM with scripts/openai_probe.py --list
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_TIMEOUT_S=120               # 083 — TOTAL budget per call (was a 60s per-attempt read timeout)
OPENAI_CONNECT_TIMEOUT_S=10
OPENAI_MAX_ATTEMPTS=2
USE_MOCK_OPENAI=1
OPENAI_VISION=1                    # 0 = REFUSE image turns (never silently drop one)
OPENAI_USE_COMPLETION_TOKENS=0     # 1 for reasoning models that reject max_tokens

# ── Kimi rail (080 — Moonshot AI, funded + VISION-capable) ─────────────────
# Probe-verified live (scripts/openai_probe.py --model kimi-k2.6 --vision):
# parallel tool calling ✓, vision ✓, takes `max_tokens` ✓.
#
# Why it matters: Kimi is the only funded rail with VISION, so under
# LLM_RAIL=kimi the 059 image turns work again (DeepSeek refuses them). It also
# restores a real fallback chain — 074 left DeepSeek primary with nothing beneath.
#
# ⚠️ HOST IS A CONFIG CHOICE. K2.6 is open-weights, so the same model is served by
# Moonshot AND by US hosts behind the same OpenAI-compatible contract. Switching is
# TWO VARIABLES, no code change — so the data-jurisdiction question stays an
# operator decision you can revisit at any time:
#   Moonshot   https://api.moonshot.ai/v1           kimi-k2.6            (default; PRC)
#   Together   https://api.together.xyz/v1          moonshotai/Kimi-K2.6 (US)
#   DeepInfra  https://api.deepinfra.com/v1/openai  moonshotai/Kimi-K2.6 (US)
# Whichever you pick is a NEW SUB-PROCESSOR: add a SECURITY.md register row and
# check its retention / training-use terms before real user holdings flow through it.
KIMI_API_KEY=REPLACE
KIMI_MODEL=kimi-k2.6
KIMI_BASE_URL=https://api.moonshot.ai/v1
KIMI_TIMEOUT_S=120                 # 083 — TOTAL budget per call. 60 was the timeout that
                                   # produced the empty "kimi request failed:" error live.
KIMI_CONNECT_TIMEOUT_S=10
KIMI_MAX_ATTEMPTS=2
USE_MOCK_KIMI=1
KIMI_VISION=1                      # 0 = REFUSE image turns (never silently drop one)

# Primary rail select. anthropic (default) | openai | deepseek | kimi. An unknown
# value fails SAFE to anthropic and logs loudly.
#
# 080 — `kimi` is the recommended primary while the frontier rails are unfunded:
# it is funded, vision-capable (unlike DeepSeek), and does PARALLEL tool calls
# (fewer loop iterations against MAX_TOOL_ITERATIONS). DeepSeek then sits beneath
# it as a genuine fallback for the first time.
#
# 074 — `deepseek` makes DeepSeek the PRIMARY (not just the fallback). It is the
# bridge for when BOTH frontier rails are credit-blocked (Anthropic exhausted,
# OpenAI 429 insufficient_quota) and DeepSeek is the only funded model. Because
# DeepSeek is the last resort, NOTHING falls back beneath it: if it fails, the
# turn errors. No vision — image turns are refused with a clear message, not
# silently dropped. Revert to `openai` (preferred; billing is easier to top up
# than Anthropic) or `anthropic` the moment a frontier rail is funded.
LLM_RAIL=anthropic

# ⚠️ SET EVERY VAR IN THIS BLOCK ON **BOTH** RAILWAY SERVICES — the web backend
# AND the briefing cron. They are separate services built from one Dockerfile and
# do NOT share variables. Setting LLM_RAIL on web only leaves the daily brief
# calling the dead rail every morning — exactly the bug 070 was written to fix.

# ─────────────────────────────────────────────────────────────────────────────
# 073 — per-call OUTPUT token cap. Unset = 4096 (chat) / 1024 (brief), i.e. the
# values that were hardcoded pre-073, so leaving these unset changes nothing.
# A per-rail override wins over the general cap:
#   OPENAI_MAX_OUTPUT_TOKENS / ANTHROPIC_MAX_OUTPUT_TOKENS / DEEPSEEK_MAX_OUTPUT_TOKENS
#   OPENAI_BRIEF_MAX_OUTPUT_TOKENS / …
# Non-numeric or <= 0 is ignored with a warning (fail-safe).
# LLM_MAX_OUTPUT_TOKENS=4096
# BRIEF_MAX_OUTPUT_TOKENS=1024

# ⚠️ INPUT is NOT bounded by tokens anywhere in this codebase — context is
# bounded by CHARACTERS and by iteration count. There is no tokenizer (raw httpx,
# no provider SDK), so these are the only levers if you pin a model with a
# smaller context window than Anthropic's 200K. Unbounded TOOL RESULTS are the
# real risk term, not history.
CHAT_HISTORY_MAX_MESSAGES=20
CHAT_HISTORY_MAX_CHARS=12000
# MAX_TOOL_ITERATIONS=10           # raise if the model won't emit PARALLEL tool
                                   # calls — each tool then costs its own iteration
